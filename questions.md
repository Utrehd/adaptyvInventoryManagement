| # | Core project question | First endpoint(s) | DB design / entities |
|---|---|---|---|
| 1 | **Do we have enough materials to complete planned lab tasks?** | `POST /v1/workflow-runs/{workflowRunId}/feasibility-checks` | `WorkflowRun`, `WorkflowStepRun`, `Recipe`, `RecipeStepInput`, `InventoryLot`, `InventoryReservation` |
| 2 | **Do we have enough material for expected QC re-runs?** | `GET /v1/workflow-runs/{workflowRunId}/material-plan?includeReruns=true`<br>`POST /v1/workflow-runs/{workflowRunId}/feasibility-checks` | `WorkflowRun.plannedRerunCount`, `RecipeStepInput`, `InventoryLot`, `InventoryReservation` |
| 3 | **Do we have enough reusable target protein, or should we order more?** | `GET /v1/inventory/availability?materialId=...&projectId=...`<br>`GET /v1/materials/{materialId}/inventory-lots?includeReservations=true&includePolicy=true` | `Material`, `MaterialTypePolicy.isReusable`, `InventoryLot`, `InventoryReservation`, `Project` |
| 4 | **Are these materials still viable / not expired?** | `GET /v1/inventory-lots/expiring?before=...`<br>`GET /v1/inventory-lots/{lotId}` | `InventoryLot.expiresAt`, `InventoryLot.status`, `MaterialTypePolicy.requiresExpiry` |
| 5 | **Where are these materials stored in the lab?** | `GET /v1/materials/{materialId}/inventory-lots?includeLocation=true`<br>`GET /v1/storage-locations/{locationId}/contents?includeNested=true` | `StorageLocation`, `Container`, `ContainerPosition`, `InventoryLot.containerPositionId` |
| 6 | **Which process step uses or creates each material?** | `GET /v1/workflow-runs/{workflowRunId}/material-plan`<br>`GET /v1/inventory-lots/{lotId}/events` | `Recipe`, `RecipeStep`, `RecipeStepInput`, `RecipeStepOutput`, `WorkflowStepRun`, `InventoryEvent` |
| 7 | **How much material was consumed, moved, produced, or discarded?** | `POST /v1/inventory-consumptions`<br>`POST /v1/inventory-productions`<br>`POST /v1/inventory-transfers`<br>`POST /v1/inventory-discards` | `InventoryEvent`, `InventoryTransaction`, `InventoryLot.currentQuantity` |
| 8 | **How do we handle different material types without hardcoding everything?** | `GET /v1/material-type-policies` | `MaterialTypePolicy`, `Material`, `InventoryLot` |